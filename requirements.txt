# Core app
streamlit>=1.35
pandas>=2.2
numpy>=1.26
scikit-learn>=1.3
imbalanced-learn>=0.11
python-dotenv>=1.0

# LLM / agents (only keep if you're actually using them)
openai>=1.40
langchain-core>=0.2
langchain-openai>=0.2
langgraph>=0.2

# Optional plotting
matplotlib>=3.8

# Remove Flask unless you truly run a Flask server (Streamlit doesn't need it)
# flask
